---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: multicluster-mesh
  namespace: open-cluster-management
  labels:
    app: multicluster-mesh
spec:
  selector:
    matchLabels:
      app: multicluster-mesh
  replicas: 1
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: multicluster-mesh
      labels:
        app: multicluster-mesh
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - command:
        - /multicluster-mesh
        args:
        - --leader-elect
        image: quay.io/morvencao/multicluster-mesh:latest
        name: multicluster-mesh
        securityContext:
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        lifecycle:
          preStop:
            exec:
              command:
              - /bin/sh
              - -c
              - /usr/local/bin/prestop.sh
        resources:
          limits:
            cpu: 500m
            memory: 128Mi
          requests:
            cpu: 10m
            memory: 64Mi
      serviceAccountName: multicluster-mesh
      terminationGracePeriodSeconds: 10
